"""Setup script."""

from setuptools import setup

setup(
    name="multi_arm_assembly",
    version="0.1.0",
    packages=["multi_arm_assembly"],
    include_package_data=True,
    install_requires=[
    ],
)